output/
.cache/
*.npz
.env

# Python
__pycache__/
*.pyc

# Houdini (scene files only; unpacked .hda directories are tracked)
*.hiplc
*.hip
backup/
# Exclude packed binary HDA at repo root (only hda/ unpacked format is canonical)
/vb_kimodo_motion_1.1.hda

# build_skin.py artifacts at repo root (embedded into the HDA as sections instead).
# Anchored to root so the copies embedded under hda/ stay tracked.
/skin.bgeo.sc
/apose.bgeo.sc
