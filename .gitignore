output/
.cache/
*.npz
.env

# Python
__pycache__/
*.pyc

# Third-party Python put on PYTHONPATH by the package file (see the setup guide).
vendor/

# Houdini (scene files only; unpacked .hda directories are tracked)
*.hiplc
*.hip
backup/
# Exclude packed binary HDA at repo root (only houdini/otls/ unpacked format is canonical)
/vb_kimodo_motion_1.1.hda

# build_skin.py artifacts at repo root (embedded into the HDA as sections instead).
# Anchored to root so the copies embedded under houdini/otls/ stay tracked.
/skin.bgeo.sc
/apose.bgeo.sc
